import React, {ReactElement} from "react";

type MovieItemProps = {
    props: {
        title: string 
    }
}

function MovieItem({props}: MovieItemProps): ReactElement {

    return (
        <>
            <div>
            </div>
        </>
    )
}

export default MovieItem;
